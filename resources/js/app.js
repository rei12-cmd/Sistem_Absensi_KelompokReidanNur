// resources/js/app.js

// Import bootstrap.js jika ada (untuk Laravel default, biasanya ada di resources/js/bootstrap.js)
import './bootstrap';

// Jika ingin Alpine.js (misalnya untuk dropdown di navbar), aktifkan baris berikut:
// import Alpine from 'alpinejs'
// window.Alpine = Alpine
// Alpine.start()

console.log('app.js loaded (Vite + Tailwind)');
